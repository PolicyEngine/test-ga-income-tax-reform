[project]
name = "backend"
version = "0.1.0"
description = "GA Income Tax Reform Calculator - Modal serverless backend"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.135.1",
    "modal>=1.3.5",
    "policyengine-us==1.225.0",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
]
